terraform {
  backend "azurerm" {
    resource_group_name  = "terraform-state-rg"
    storage_account_name = "tfstateerprodadvisor"
    container_name       = "tfstate"
    key                  = "sandbox.tfstate"
  }
}
